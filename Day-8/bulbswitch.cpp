int bulbSwitch(int n) {
        return int(sqrt(n));
    }
    // here for every number to get the bulb on or off 
    //depends on the number of factors of that position
    // only a perfect square has odd number of factors.
    //so calculationg number of perfect sqare till given n 
    // thats equal to square of number